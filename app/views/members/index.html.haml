- if current_member.grade == 3
  - if @gradelessIndex
    .aaa
      = link_to "メンバー一覧", members_path(gradeless: nil)
  - elsif @gradeLess>0
    .aaa
      グレード未登録のメンバーがいます
      = link_to "未登録一覧", members_path(gradeless: 1)
- @members.each do |m|
  = link_to member_path(m.id) do
    = m.name
